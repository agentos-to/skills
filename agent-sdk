#!/usr/bin/env python3
"""agentOS Skills SDK — no install needed."""

import sys
from pathlib import Path

# Add sdk/ to Python path so 'from agentos import ...' works
sys.path.insert(0, str(Path(__file__).parent / "_sdk"))

from agentos.cli import main
main()
